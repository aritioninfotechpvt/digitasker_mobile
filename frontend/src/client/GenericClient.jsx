import React from 'react';import AppLayout from '../layouts/AppLayout';import {Card} from '../components/ui';export default function GenericClient({title}){return <AppLayout role='client' title={title}><Card><h2>{title}</h2><p className='muted'>Client-facing frontend section with dummy data ready for API mapping.</p><div className='emptyState'>Charts, reports, campaign actions, billing and message workflow will render here.</div></Card></AppLayout>}
