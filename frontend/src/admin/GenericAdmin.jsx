import React from 'react';import AppLayout from '../layouts/AppLayout';import {Card} from '../components/ui';
export default function GenericAdmin({title}){return <AppLayout role='admin' title={title}><Card><h2>{title}</h2><p className='muted'>Frontend module prepared with dummy structure for backend API integration.</p><div className='formGrid'><input placeholder='Search / Filter'/><select><option>All Statuses</option><option>Active</option><option>Pending</option></select><input type='date'/><button className='primary'>Apply Filters</button></div><div className='emptyState'>Demo data table / workflow area for {title}</div></Card></AppLayout>}
